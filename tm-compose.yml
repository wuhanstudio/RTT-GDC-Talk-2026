version: "3.3"

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./docs:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.tm.rule=Host(`tm.wuhanstudio.uk`)"
        - "traefik.http.routers.tm.entrypoints=web"
        - "traefik.http.services.tm.loadbalancer.server.port=80"
        # TLS
        - "traefik.http.routers.tms.rule=Host(`tm.wuhanstudio.uk`)"
        - "traefik.http.routers.tms.entrypoints=websecure"
        - "traefik.http.routers.tms.tls.certresolver=myhttpchallenge"
        # Redirect
        - "traefik.http.routers.tm.middlewares=https_redirect"
        - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
    networks:
      - traefik-public
networks:
  traefik-public:
    external: true
